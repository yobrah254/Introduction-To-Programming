#include <stdio.h>

int main() {
    int a;

  printf("Enter the no.");
  scanf("%d",&a);
  if(a%5==0)
  {
    printf("no is divisible by 5");
  }
  else
    printf("no is not divisible by 5");

    return 0;
}
