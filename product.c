//program to calculate producr of two integers
#include <stdio.h>
int main()
   {
	int b, k, result;
    printf("\nInput the first integer: ");
    scanf("%d", &b);
    printf("\nInput the second integer: ");
    scanf("%d", &k);
    result = b * k;
	printf("Product of the above two integers = %d\n", result);
     return 0;
}
